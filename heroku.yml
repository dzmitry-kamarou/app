setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config: "null"
build:
  docker:
    web: Dockerfile
    worker: diamond/Dockerfile
  config: "null"
release:
  command:
    - ./rerun.sh
  image: diamond
run: "null"
