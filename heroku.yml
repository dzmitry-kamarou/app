setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
build:
  docker:
    web: Dockerfile
    worker: diamond/Dockerfile
  config:
release:
  command:
    - ./rerun.sh
  image: diamond
run: